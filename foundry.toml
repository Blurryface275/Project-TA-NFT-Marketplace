[profile.default]
src = "contracts/src"
out = "contracts/out"
libs = ["contracts/lib"]
solc_version = "0.8.36"
evm_version = "prague"

optimizer = true
optimizer_runs = 200

gas_reports = ["TicketContract", "MarketplaceContract"]
